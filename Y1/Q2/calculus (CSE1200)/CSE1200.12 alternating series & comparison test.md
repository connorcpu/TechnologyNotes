# alternating series test
- om en om positief en negatief, like $(-1)^n \frac 1 n$
- als series satisfies $\displaystyle\sum_{n=0}^\infty b_n$
   - $\forall n(|b_{n+1}| \leq |b_n|)$
   - $\displaystyle\lim_{n\to\infty}|b_n| = 0$

# comparison tests 
- suppose $\sum a_n$ & $\sum b_n$
1. if $\sum b_n$ convergence and $\forall n(a_n \leq b_n)$ then $\sum a_b$ also converges
2. if $\sum b_n$ is divergent and $\forall n(a_n \geq b_n)$ then $\sum a_n$ also diverges
- not actually $\forall n$ but forall n larger than some number N in natural numbers (we don't care about the start only about the tail) $\exist N\in \N\space\forall n (N > n \to C)$ where C means comparison test holds
## limit comparison test
- suppose $\sum a_n \land \sum b_n$
- if $\displaystyle\lim_{n\to\infty} \frac {a_n} {b_n} = C$
- where C is a finite positive number, then either both series converge or both diverge
- afstand s_3 tot limiet waardde
- 

# error bounds on partial sums
- $\displaystyle\int_{n+1}^\infty f(x) dx \leq s-s_n \leq \int_n^\infty f(x)dx$
- $\displaystyle s_n + \int_{n+1}^\infty f(x) dx \leq s \leq s_n + \int_n^\infty f(x)dx$

# excersizes
## 1
$\displaystyle\sum_{n=1}^\infty {(-1)^{n-1} \over n}$  
alternating? convergent?
answer: alternating, does converge by the altenating series test? [TODO] alternating series test can test convergence? 

## 2
$\displaystyle\sum_{n=1}^\infty \cos(\pi n)$  
alternating? convergent?  
defenitly alternating, converges to 1? alternating series test says maybe convergent but dunno.
divergentie test zegt convergeert helemaal niet dus al helemaal niet naar 0?

## 3
$\displaystyle\sum_{n=1}^\infty {n \over \sqrt{1+3n^4}}$  
convergent or divergent?  
answer: 

