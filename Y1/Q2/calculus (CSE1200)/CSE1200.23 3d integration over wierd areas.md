use variable grenswaarde on integraal to draw shape of the projection base.
buitenste integraal MOET vaste (on-varierende) grenswaarde hebben anders is het resultaat een functie geen antwoord

# regios
## type 1
- $a < x < b$
- $c(x) < y < d(x)$

## type 2
- $a < y < b$
- $c(x) < x < d(x)$
- it's the same but x and y swapped


- $\displaystyle\iint_A f(x,y) dA = \int_a^b \int_{g_1(x)}^{g_2(x)} f(x,y) dy dx$


- $\displaystyle\int_0^1 \int_0^{1-x^2} 5x\cos(5y)dy dx$
- $\displaystyle\int_0^1 x\sin(5-5x^2) dx$
- $\displaystyle\int_5^0 - \frac 1 {10}sin(u) du$
- $\displaystyle\frac 1 {10} \int_0^5 sin(u) du$
   - $f'(x) = x$
   - $g(x) = \sin(5-5x^2)$
- $\displaystyle [\frac 1 2 x^2 * \sin(5-5x^2)]_0^1 - \int_0^1 \frac 1 2 x^2 cos(5-5x^2) \cdot -10x dx$
