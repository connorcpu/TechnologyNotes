# implicid diffrentiation
- kan worden gebruikt voor raaklijn aan cirkel
- afgeleide van inverse van trigo funcs
- why is $x ln(3) = ln(3^x)$
- basically just call y a func of x and just keep rolling like nothing is wrong, okay?
- $\frac d {dx} \arcsin(x) = \arcsin '() = {{1}\over \sqrt{1-x^2}}$
- $\frac d {dx} \arccos(x) = \arccos '() = -{{1}\over \sqrt{1-x^2}}$
- $\frac d {dx} \arctan(x) = \arctan '() = {{1}\over{1+x^2}}$
- probably triggered on any non-y-equals "function"
- 

# newtons method (newton-raphson)
$x_m = x_{n-1} {{-f(x_(n-1)}\over{f'(x_{n-1})}}$
figure this out

# exersizes 
check slides for some derivative stuff, maaaayyyybee check the pre-lecture vid
## 1
$$
