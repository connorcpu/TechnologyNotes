# what 
- definitie van een limiet: $f'(a) = \displaystyle\lim_{h\to 0} {{f(a+h)-f(a)}\over{h}}$ if this limit exists
- tangent line is raaklijn, als je het punt weet en de richtingscoefficient weet, yada yada dit is simpel
   - $y = m(x-a)+b$ gaat door punt (a, b)
   - $y = f(a) + f'(x)(x-a)$
   - second dirivative f''(x)
   - 3e: $f^{(3)}(x)$
   - 4e: $\frac {df}{dx^4}$
- regels:
   - sum: $(f + g)' = f' + g'$
   - product: $fg' + f'g$
   - quotient (don't use): ${{fg' + f'g}\over{g^2}}$
   - chain (subsitute quotient with this): $(f(g(x)))' = f'(g(x)) \cdot g'(x)$

$(\frac f g)' = (f \cdot \frac 1 {g'})' = f' \frac 1 g- \frac {f-g} {g^2} = {{f'\cdot g - f \cdot g'}\over {g^2}}$   
in de buurt van het raakpunt is de raaklijn een redelijke benadering voor de orginele functie   
$f'(x)= \frac {\Delta y} {\Delta x}$ <=> $f'(x)\Delta x = \Delta y$ (because of the geometry of linearization)(is approximation)

# exercises
## 1
$f(x) = x^2$, $\Delta x \neq 0 \land \Delta y = f(x+\Delta x) -f(x)$
what is $\Delta y \over \Delta x$  
answer is $2x + \Delta x$ because no limit
## 2
$f(x) = |x-3|$  
$\displaystyle \lim_{\Delta x \to 0} {{f(3+ \Delta x)-f(3)}\over{\Delta x}}$  
answer is does not exist

## 3 
$f(x) = x^4-3x$
tangent line at x=-1
