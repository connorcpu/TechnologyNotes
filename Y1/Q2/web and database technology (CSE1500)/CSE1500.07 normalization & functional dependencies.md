functional dependency occurs when if two tuples agree on some set of values, they must also agree on a wider set of other values,
example: if A and B agree on startTime and endTime, they must agree on duration


- 1NF is when there are no multiple value, such as one student taking multiple courses in one table and it also cannot be composed
- 2NF 1NF and every attribute depends on the full key, not on a partial key, {hero_id, team_id, hero_name, team_name
- 2NF 1NF, there are multiple (candidate) keys and eveything depends on all of them or is done through transativity
      - user-defined: "CREATE DOMAIN 
- 3NF 2NF and no functional dependencys, nothing transative
