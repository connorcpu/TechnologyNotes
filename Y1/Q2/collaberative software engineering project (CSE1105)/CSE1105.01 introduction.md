# introduction
- first years might be signed up already?
- sebastian proksch, main guy
- odette scharenborg, professor,
- ivar de bruin, irrelavant
- physically impossible to do by yourself
- peer review
- merge requests....?
- you need to ask the TA for the meeting
- you can miss **one** meeting without excuse
- excused absences are only granted **before** the lab by a *lecturer*
- we are building a recipe manager called foodpal
- don't do users, use local config file

# course stuff
- git
   - commit early
   - commit ofter 
   - integrate branches daily
   - trunk-based is when you update so fast everything stays in-sync
   - feature branches the usefull way to do it
   - working directory is the final product of all commits
   - staging area is uncommited changes, (dirty working directory)
   - repository is the whole thing with branches etc
   - checkout composes commits into a working directory so you can image how checkout a branch or a commit works
- build tools
   - make?
   - cmake?
- build server
   - gitlab automatically compiles and runs every commit
   - import checkstyle to build server(find out how to do a backslash)
- gitlab
- maven
