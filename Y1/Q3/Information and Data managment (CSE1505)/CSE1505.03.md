# stuff
- JDBC v. JPA
   - difficulty is configuration
- relational databases don't really scale, hashmaps do
- old way to do shit is to have everything in seperate applications, using the same db
   - concurentie issues
   - security issues 
   - reliability
   - all 'buisness units' have to agree on a schema
- jdbc works with nearly every different vendor of DB's
   - prepared statements
   - use indexes
   - use transactions
      - small application, either runs entirely or not at all
- lazy load v. eager load
- use natural keys whenever you can, don't use a natural key as key when it's not the actual key in the DB
- people cutting corners is the root of all evil
