# stuff to know
- indexes
   - sparse, one index per block
   - dense, one index per record
   - primary index, sort stuff by primary index, allows binary search ETC
   - secondary index, must be dense, 
   - for point access (x=5) use hash indexes, hash the index, it gives you the block it's in
   - for point or range, use B+ trees
      - leaf nodes store data, internal nodes only navigation
      - compare key with data, all leafs point to next leaf, this means for range, find begining and end, then just extract all
      - they work like 2-4 trees
   - for multi attribute querys use K-D trees
      - binary tree but every level alternates on which atribute it splits
- SQL
- "application programming" (JDBC & JPA)
- query proccessing
   - heuristic optimization
      - basically just apply a bunch of random garbage that may or may not work but superstision say it helps.
      - rules like cartesian project should be a join or subquerys should be a join or select tuples as early as possible 
   - cost-based
      - find out every way we can do the query, then estimate for every query how long it will take, as well as taking intermediate result set size into account
      - cost of index scan is $#mt * (cost_{ix} + 1) + cost_{result}$ OR it's the the amount of blocks
         - where #mt is the expected output of the select
         - cost_{ix} is the height of the tree for K/D & B+ and 0 for hash
      - $cost_{result} = {|R\bowtie S | \over blockingfactor_{result}}$ (this is the cost of storing the intermediate result
         - where the blocking factor is the amount of tuples that fit in a block
      - cost of block nested loop join is $blocks(R) + blocks(R) * blocks(S) + costs_{result}(R\bowtie S)$ (done when no indexes, loop over one table, and nested loop over other table)
      - cost of index nested loop join is $blocks(R) + (|R| * (cost_{ix} + 1)) + costs_{result}(R\bowtie S)$ (loop over one table, use index for inner loop over other table)
      - sorted merge join cost is $blocks(R) + blocks(S) + cost_results(R\bowtie S)$ (only works if both tables are primary index-sorted by the joining attribute, therefore only useable on natural and equi)
      - hash join cost same as sorted merge join, hash the joining column for both tables, if they match the output will be the same, so sort into buckets, then compare everything per bucket, only use on equi and natural 
      - join reduction factors are based on carteasion product vs actual result size
- transaction management
   - atomicity
      - done by logging
   - consistency
      - integrity constraints
   - isolation
      - locking / timestamping
   - durability
   - 
- legal aspects


# stuff to memorize
- relational algebra
   - $\sigma$ = select (where atribute, works on rows)
   - $\pi$ = projection (select atribute, works on columns) (projection can't handle duplicates)
   - U = union, merge 2 intermediate result sets
   - $\cap$ = intersection, only output that which is in both inputs
   - \ = set difference, output the union minus intersection
   - $\rho$ = rename, rename a column
      - table can be renamed from R to S using $\rho_s (R)$
      - rename attributes from a,b to A,B by $\rho_{(A,B)} (\pi_{a,b}(R))#
      - both is also an option
   - x = cartesian product
   - $\bowtie$ = join 
      - theta join ($\bowtie$_{<condition>}$) joins both tables where the condition is met
      - natural join ($\bowtie$) is equi join but removes the duplicate attribute in the output
         - if tables both have an attribute with the same name the join will join on that
      - equi join ($\bowtie_{condition}$) like theta but only equals as condition
   - $\gamma$ = group by and filter on
- SQL
   - WITH table AS (subquery) for temp table (roccelated means inner query has to be re-run everytime)
   - HAVING filters out groups on given conditition
   - LIKE for string operations
   - CROSS JOIN
   - UNION EXCEPT INTERSECT, add ALL to not eliminate duplicates
   - (NOT) IN, EXIST: filter based on if tuple is in subquery
   - VIEW, same as WITH, if you add materialized you can also refresh it

# topics to learn
- column store vs row store, columns better for agregate statistics
   - rows better if you need most attirbutes
- RAID
   - raid 0, strip the disks, so 2 disks gives double the bandwith but 1 failure means all data lost
   - raid 1, mirroc disks, 1 failure means just use backup disk
   - raid 2, strip on bit, or byte level, like raid 0 but on bit level
   - raid 3, stripe on bit/byte level, then add 1 disk for parity, if any SINGLE disk fails you can comensate using the parity disk, parity disk can also fail
   - raid 4, add a parity disk on the block level
   - raid 5, to avoid parity disk bottle neck, distribute the parity blocks to a disk which doesn't have the data it's parity'ing
- forumlas
   - cost of a relation linear scan: cost = #blocks(R), or cost < #blocks(R) if theres a primary range, you can stop early
   - cost of a binary search: cost = log_2(#blocks(R)), this plus linear scan if you are reading a range
   - cost of index scan: costForIndex + costForRetrievingResults
      - trees:
         - heightOfTree if everything on disk 
         - heightOfTree - 2 if root and first layer are in mem, rest on disk
         - 0 if everything is in mem
      - hash:
         - 0
   - other cost of index scan:
      - basic: cost = expectedNumTuples * (cost_ix + 1)
      - advanced: cost = min(expectedNumTuples * (cost_ix + 1), blocks(R)) //avoids access 1 block with several tuples several times
   - cost of intermediate result: cost(r \bowtie s) = |r \bowtie s | / tuplesPerBlock
   - join costs
      - block nested loop (for every block in R, read all blocks of S): cost = #blocks(R) + (#blocks(R) * #blocks(S)) + cost(R \bowtie S)
      - index nested loop (use index for inner loop): cost = #blocks(R) + (|R| * (cost_ix + 1)) + costs_results(R \bowtie S)
      - sorted merge join (only works if both are sorted by merge attribute(rare)): cost = #blocks(R) + #blocks(S) + cost(R \bowtie S)
      - hash join (only usabel for equi and natural, hash all tuples by join attribute, the compare each tuple per bucket): cost = #blocks(R) + #blocks(S) + cost(R \bowtie S)
- serializablitly
   - so a serial schedual just executes one after the other. 
   - a serializable schedual has the equal effects to a serial one.
      - (do note that they cannot be executed in parallel in the CPU sense as it merely executes parts of transactions at a time)
   - a conflict serializable schedual is a schedual where swapping the order of the transactions is able to achieve a serial schedual
      - opperations conflict on a 1-1 scale, (one write is compared with one read); they confilct if:
         - belong to a different transaction 
         - operate on the same data (read or write the same var)
         - one of them writes
- logging 
   - logging should be idempotent, multiple (partial) recoveries should net the same outcome
   - undo logging: write to disk, then commit, if abort, just put back old values
   - redo logging: commit, then write to disk.
      - store NEW value
      - if no commit in log file, nothing was written, reschedual to rerun
      - if there is a commit. compare each value on disk with the new value, update any wrong values
   - undo/redo logging
- transaction management
   - dirty read: read uncommited data that gets aborted
   - dirty write: commit the write of data based on dirty read
   - non-repeatable read: transaction reads twice, inbetween other transaction updated data
   - phantom read: like non-repeatable but with the amount of query results differing
   - write skew: two transactions read same data and write different data in a way that violates integrity constraints
   - lost update: two transactions write based on data they read before the other wrote. E.G. 2 transaction that increment a var. both read 10, 11 written twice.
   - snapshot isolation solves everything but dirty write and write skew.
   - serializability solves all but is slow
   - 2PL
      - all locks are acquired before the first lock is released
      - growing phase (lock granting)
      - shrinking phase (lock releasing)
      - 2PL scheduals are guarenteed to be serializable since the are a propper subset of conflict serializable
      - however: deadlocks
   - timestamp-based concurrency control (optimistict if conflicts are rare)
      - just perform validation at the end
      - if operations conflict the oldest transaction must come first
      - every mutated data is marked with the timestamp of the transaction who did it
      - if when trying to do an operation the timestamp on the data is younger than the transaction trying to mutate, you'd be trying to read a future value -> abort & retry
- distributed data 
   - coordinator sends 'prepare'
   - all node workers do the transaction up to commit 
   - all node workers send ready.
   - if all sent ready the coordinator sends out commit.
   - all node workers send out ack 
   - if at any point failure, send out rollback command
   - timestamps are hard to sync, maybe just dont
- legal aspects 
   - 
- relational algebra 
- advanced SQL
- query proccessing
- indexes
- noSQL
   - horizontal scaling is hard, especially across geographic regions
   - CAP theorem states you can only ever have either consistancy, partition tolerance, or availability, pick 2, not 3
      - consistency is usually sac'ed
   - if consistancy is REQUIRED DO NOT USE NOSQL
   - noSQL DOES NOT SUPPORT ACID, instead they are built to scale
   - noSQL uses documents instead of relations, more like JSON, every "row" has attributes that may or may not exsist, no guarentees
      - instead of linking to other tables, add it as a sub-attribute, like alice having 2 orders, the alice doc includes her order info
   - this can couse unbounded data growth, like a user with a milion orders
   - this does not support many-to-many relations
   - this means a commonly ocurring object has to be duplicated, like every student defines the attributes of every course they take
   - difficult to query sub-attributes
