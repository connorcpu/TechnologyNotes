- stuff
   - lineare transformaties
      - t(u+v) = t(u) + t(v)
      - t(cv) = c t(v)
   - meest nuttige voor computer graphics dingen
- range (possible outputs, subset of the codomain (for example $\R^2$)) is the span of the columns, also known as the column space
- to get the standart matrix of a linear transformation, apply the transformation to the unit vectors ((0,1), (1,0)); the result will be the columns of the matrix that applies the linear transformation
   - the order of the columns is just the x_1 unit vector (1, 0, ..., 0) first, then the x_2 unit vector (0, 1, 0, ..., 0)
