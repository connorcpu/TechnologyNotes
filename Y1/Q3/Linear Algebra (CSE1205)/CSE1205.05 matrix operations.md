# stuff
- $e_i$ vectoren hebben maar een 1 op plek i en wijzen dus langs een as
- you can add up matrices if the rows and columns are the same size.
- voor vermenigvuldinging moet A dezelfde hoeveelheid rijen hebben als B collumnen heeft
   - als A M x N is en B is N x P dan is de samenstelling M x P
- matrix matrix multiplcation is generalisation of matrix-vector because vector is 1xn matrix
- eerste collumn van nieuwe matrix is matrix vector van matix A keer eerste colunm van B
- $s \circ t(x) = s(t(x))$
   - so if $s \equiv a \land t \equiv b$
   - then $ABx = s \circ t$


- matrix scaling: just apply c to every element
- matrix addition: just add every corisponding element together
- matrix multiplication: just multiply A by every column (as vector) of B

- matrix transpose: the columns are gonna make rows
