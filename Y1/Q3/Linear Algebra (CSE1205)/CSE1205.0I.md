- kleinste kwadraten:
   - projection: $Ax = \hat b$; solve as augmented matrix
   - $A^t$: $A^t A{\hat x} = A^t b$
   - diagonalisation: reason for powers of matrices
   - orthogonality, why? makelijker om mee te rekenen?
   - gramm-schmidt, used for turning a basis into a orthogonal basis
      - basis = $\{b_1, ..., b_p\}$
      - project the vectors onto the new basis
   - geometric multiplicity
      - subtract eigenwaarde van diagonaal
      - reduceer matrix
      - hoeveel columnen zonder pivot
      - geometrische multiplicity is hoeveel dimentionaal de eigenspace is
   - how to find standard matrix of a transformation
      - apply the transformation to the unit vectors
   - projection formula needs to be applied to orthogonal basis
   - lineare afhankelijkheid in de echelon vorm is hetzelfde als in de originele
   - A=PCP^{-1}, A & C zijn similair meaning they have the same eigenwaardes
      - solve characteristic polynomial for lambda
- transforming determinants
   - multiplying a row with a constant k also multiplies the determinant: det(B) = k det(A) where B is A but with any row multiplyied by k
   - det(A^T) = det(A)
   - det(A^{-1}) = {1 \over det(A)}
   - det(AB) = det(A) * det(B)
   - switching rows negates the determinant ( \cdot -1)
   - adding or subtracting rows does not influence the determinant
   - power of matrix just powers the determinant (B*B*B)
   - det(a^3) = (det(A)^3
   - det(2a) = 2^n det(a) (where a = n x n matrix)
   - if u^-1 = u^T then orthogonal
   - to project ${V \cdot U \over V \cdot V} U + {V \cdot U_2 \over U_2 \cdot U_2}$
   - to orthogonalize, subtract the projection from the original, duh
   - matrix A & B \\ -B & A has complex eigenvalue A+Bi
   - the orthogonal complement of the column space of a matrix is the null space of the transpose
   - to diagonalize a rotation matrix, the C follows standart complex eigenwaarden structure, the matrix P is composed of the real part of the eigenvector, then the imaginairy part of the eigenvector



- question #1:
   - question: $P = \begin{bmatrix} 1 & 1 & 0 & 0 \\ 1 & 1 & 1 & 0 \\ 0 & 1 & 1 & 1 \\ 0 & 0 & 1 & 1 \end{bmatrix} \land C = \begin{bmatrix} 6 & 0 & 0 & 0 \\ 0 & 3 & 3 & 0 \\ 0 & 3 & 3 & 0 \\ 0 & 0 & 0 & 3 \end{bmatrix}$
   - $A=PCP^{-1}
   - find all eigenvalues of A
   - answer: $0, 3, 6$
   - omdat:
      1. $\det(C-\lambda I) = (6-\lambda)(3-\lambda)((3-\lambda)^2 - 9) = 0
      2. solve for lamda
   - find a basis of the corresponding eigenspaces
      1. subtract eigenvalue from C
      2. repeat for each eigenvalue
      3. solve the homogenous system
   - answer: (0,1,-1,0) for 0


# still todo at least
- rank of matrix: dim(col(A))
- what is singular and how do we determin if a matrix is singular: not invertable, therefore, determinant = 0
- matrix algebra: to add just add, A_i,j to B_i,j
- discreet dynamical systems: $c_1 \cdot \lambda_1^k \cdot \vec\lambda_1 + \dots + c_n \cdot \lambda_n^k \cdot \vec\lambda_n$
- how to find orthogonal vectors: dot product = 0 -> $a_1 \cdot b_1 + a_2 \cdot b_2$
- gramm-schmidt
- best fit-line: least squares problems, multiply both sides with transpose of matrix but what equations
