- red-black tree
- avl tree
   - binary search tree 
   - difference in height is at most 2 between every left child compared to it's sibling right child
   - the thing of note are single and double rotations to restore the height constraint
   - single: considering X-Y-Z with Z as root and the Y side being inbalanced, make Y the root and give one child of Y to Z
   - double: similar story but X as root, one child to Y one to Z
   - to remove from AVL, start by swapping with max element from left tree, then remove and rebalance using rotations
- hash-map implementation
   - usually consists of one mathematical function and one compression function
   - math function can output anything so compression function is used to bring it within the range of the hashtable
      - simlpe f(x) = x mod N works well if N is prime (N is size of hashtable)
      - more complex ((Ax+b) mod P) mod N, where p >= N and P is prime. A and b sould be between 0 and P-1
   - collision can still happen and should be resolved:
      - separate chaining: implement a secondary function to resolve the collision, the advice is an unordered list because the number of collisions in any given bucket is small and varies
      - open addressing: whenever a collision occurs, just try one address further, also loop around once you reach N
- what is load factor
- graph representations
   - edge map 
      - keep a list of every vertex and every edge, the entry for an edge schould store it's identity and which vetices it's connected to
   - adjacency list
      - store for each node what edges it's attached to
   - adjacency map 
      - store for every node which nodes it is connected to and via what corrisponding edge it's connected
   - adjacency matrix
      - on one axis have the in-node, on the other have the out-node. then in the corrisponding cell put what edge facilitates that connection
- what is kruskal's algorithm
   - minimum spanning tree
   - every node is a cluster
   - find the smallest edge that's not inside a cluster
   - join the 2 adjacent clusters with that edge 
   - mark that edge as part of the minimum spanning tree
- (2,4) trees
- recurrance equations
- in-place quick-sort

# to rememeber
- to delete AVL tree's: swap with max element left tree
- sorting times:
   - selection sort: always n(n-1)/2 comparisons
   - merge: always n log (n) comparisons
- inserting 2-4 tree, whenever you reach a 3 node, break it up, move middelvalue to first value in parent node
- inserting into red-black, insert at BST leaf node, as red,
   - if parent black: no violation
   - if parent RED: VIOLATION
      - parent sibling black: right/left rotate (or a double one if going from grandparent to parent to child swtiches dir
      - parent sibling RED: colour parent and parent-sibling BLACK, colour grandparent RED, repeat up tree
- delting from red-black, 
   - if removing RED: nothing is wrong
   - if removing BLACK: good luck ig
