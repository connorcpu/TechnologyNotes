- get a data generator
- get a shrinker, gives you minimal reproducable example of a certain bug
- jqwik for java
- round-tripping, methods that do their inverse
- commutativity, the order of operations does not matter
- invariants, design by contract
- idempotence: sorting a list twice should yield the same as sorting once
- structural induction
- hard to comput, easy to verify: mazes, prime number factorization
- differential testing: comparing your implementation to a reference implementation, test against an known working algorithm
- metammorphic testing: gay
- catastrophic failure: see where the program crashes
- propperty based testing does not have the programmer come up with tests but an external tool

# practical
- @Property instead of test
- @ForAll int i auto fills i for any integer, used for method parameters
- @IntRange(min=0, max=100)
- @Report(Reporting.GENERATED) for logs
- @UpperChars
- @StringLengh(max=10)
- @AlhpaChars (on a string parameter) will restrict to alpha numeric latin chars
