# stuff 
- random variables
   - supose you flip coin 5 times, 1 for heads, 0 for tails. the probablility of throwing 3 heads is the sum of the throws X = 3
   - so we do $X: \Omega \to \R$
   - in this example the mapping function is $x(w) = \sum x_i$
   - discrete uniform distribution
   - if your mapping function output is uniformly distributed with d options gives 1/d chance per outcome
- density function
   - given some function that gives you a probablility curve, you can't take the chance at 1 point anymore because the chance of a continues function is 0.
   - instead take the integral of left limit to right limit.
   - a function is a desnsity function if the integral from limit to limit is equal to 1 AND it never takes negative values
- normal distrubution
   - average value in the middle
   - standard deviation is how spread out that shit is
   - the chance of being 1 standard deviation away is 68%
- cumulative distribution function

# excersizes
## 1
- question
- answer
- why:
