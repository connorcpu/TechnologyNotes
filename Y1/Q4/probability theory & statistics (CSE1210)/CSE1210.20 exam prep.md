- conditional probability
   - p(c|a) = gwn de intersect maar gedeeld door A
   - grab a ven-diagram
   - bayes rule: $p(b|a) = {p(a|b)p(b) \over p(a|B)p(a)+p(b)}$ wat?
- adding up RV's with distributions
   - convolutie integraal $f_Z(z)\int_\infty^\infty f_X(x)f_Y(z-x) dx$ voor Z = X + Y
- confidence interval on normal distribution 
- method of moments
   - $\alpha \over x^{\alpha + 1}$ (pareto distribution)
   - gebruikt om estimators te vinden
   - gebruikt om parameter van een verdeling te vinden als je al weet welke verdeling
   - eerste moment is $\mathbb E[x]$ van de verdeling, dus voor pareto $\alpha \over {\alpha -1}$
   - nu kunnen we $\mathbb E[x]$ uitdrukken in $\alpha$
   - herschrijf naar $\alpha$ in termen van $\mathbb E[x]$ want $\mathbb E[x]$ is makkelijk emperisch te schatten (gemiddelde)
- bias van schatter is verschil in verwachting van de schattings waarde en de daadwerkelijke alpha
- generating functions
   - used to calculate sums of RV's 
   - generating function of X $g_X(s) = \mathbb E[s^x] = \displaystyle\sum_0^\infty s^k p_x (k)$ $(X \in \N)$ wat dus een polynoom geeft
   - eerste moment is de verwachtingswaarde
   - verdeling word gekenmerkt door zijn momenten
- $\mathbb [x] = \int_{-\infty}^\infty x f_x(x)dx$
- standaard fout op normaal verdeling is $\sigma \over \sqrt{sample size}$
- student-T verdeling
- mean squared error
- voor lineare regressie word matrix methode aangeraden maar word waarschijnelijk niet gevraagd
   - vooral output interpeteren



- median means F(m) = 1/2
   - F(q) = p means q is a quantile at level p
   - lower quartile means F(a) = 0.25
- hypothesis testing, false positive is type 1, false negative is type 2
- p-values are the chance of observing this data, or more extreme data
- for student-t, column is degrees of freedom.
   - then take $\alpha \over 2$ for p because you're checking on 1 side of the normal usually
   - then if your t value is LOWER than the listed value, reject
- maximum likelihood:
   - basically, how likely is the dataset for a given parameter, gives a function which maps parameters to likelihoods
   - take this function, find the maximum to find the most likely value of your parameter
   - to do this, multiply the density/mass function with all observed values together: $L(\theta) = f(x_1) \cdot f(x_2) \cdot \dots \cdot f(x_n)$
   - then its usually easier to take the ln of everything, or just any log for that matter. giving you $\mathcal l(\theta)$
   - this function has the same maximum as $L(\theta)$ but is usually easier to compute with
   - then derive and set equal to 0. then check if what you found is actually a maximum
   - MLE is asymptotically unbiased
- student-T:
   - to find a confidance interval, you want to know the specify how certain you want to be $\mu$ is in the given interval
   - to this end. we get a dataset. then select an $\gamma$. usually 0.95. alpha will be $1-\gamma$ so 0.05 usually
   - then the student-T table allows you to put in the area you want under the tail curve, and it gives you the ciritical value.
   - so since you want half your alpha on the right tail and half on the left tail. you are looking for p=0.025 usually.
   - the table then gives you a value. 
