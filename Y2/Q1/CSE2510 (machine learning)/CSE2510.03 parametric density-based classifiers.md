# stuff
- i may have missed some important stuff
- missed:
   - objects
   - feature vectors
   - feature space
   - decision boundary
   - closs conditional distribution
   - posterior probability
   - bayes classifier
   - bayes rule
- classification
   - feauture space based of feature vector 
   - goal is to divide feature space into different output labels
   - we always use p(x) also when talking about discrete
   - data goes in a matrix
   - feature vector is a column matrix
   - compute the probability it's a class A and that it's a class B. then assign the highest class
- generative vs descrimantive
   - generative estimates the conditional densities
   - called generative because it is possible to generate a sample
   - discriminate, directly estimates the posteriors.
   - eg: given measurements, for persons height, how to estimate the prob that we are talking about a women given a given height
- the more features i have the better i can describe the object, however the more dimentions i have so i need way more samples
- there is an optima
- non-parametric means just make a historgram from your dataset
- parametric means we assume a given distribution and we estimate the parameters of the distribution
- gausians (normal dists) are now m-dimensional. covariance matrix is very important. mean is now a vector, variance ofc a matrix with individual variances being on the diagonal
- multi-dimentional gausian: $p(x) = {1 \over \sqrt{(2\pi)^M det(\Sigma)}}\exp(-\frac 1 2 (x-\mu)^T \Sigma^{-1}(x-\mu)$
- eigenvectors of covariance matrix scaled by eigenvalues $\sqrt{\lambda}$
- you will be given a dataset and be expected to apply maximum likelihood since it's the best
- generalize maximum likelihood to multi-dimentional
- mean: (2.5, 0) variance x_1 = 0, x_2 = 0?, covariance = 0
- you can sum gausians to create new distributions for data that does not fit a gausian nicely
